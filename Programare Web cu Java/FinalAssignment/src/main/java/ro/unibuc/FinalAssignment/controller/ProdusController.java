package ro.unibuc.FinalAssignment.controller;

import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;
import ro.unibuc.FinalAssignment.dto.produse.AddRequestProdus;
import ro.unibuc.FinalAssignment.dto.produse.ProdusEntry;
import ro.unibuc.FinalAssignment.service.ProdusService;

import java.util.List;

@RestController
@RequestMapping("/produse")
public class ProdusController {
/*
    private final ProdusService produsService;

    public ProdusController(ProdusService produsService) {
        this.produsService = produsService;
    }


    @ResponseStatus(HttpStatus.NO_CONTENT)
    @DeleteMapping
    public void removeProduseScumpe(@RequestParam(value = "pretThreshold") double threshold) {
        produsService.removeProduseScumpe(threshold);
    }


 */
}
